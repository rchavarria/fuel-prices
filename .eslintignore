# ignore a JavaScript file for the browser
src/js/app.js

.eslintrc.js

# ignore Jest config file
jest.config.ts

# don't ever lint node_modules
node_modules
# don't lint build output (make sure it's set to your correct build folder name)
dist
